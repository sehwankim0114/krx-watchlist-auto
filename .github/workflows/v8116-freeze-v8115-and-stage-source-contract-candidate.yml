name: Freeze V8115 evidence and stage source contract candidate v8.11.6 validation-fix

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/v8116-freeze-v8115-and-stage-source-contract-candidate.yml"

permissions:
  contents: write

concurrency:
  group: v8116-stage-source-contract-candidate
  cancel-in-progress: false

jobs:
  stage:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout latest main
        uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies
        shell: bash
        run: |
          set -euo pipefail
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Capture protected hashes and lineage
        shell: bash
        run: |
          set -euo pipefail

          git merge-base --is-ancestor             c6758c8f9a269d2907ee17fb1726fb3de37362dc HEAD

          sha256sum             config/investment_score_policy_v880.json             investment_score_source_enricher_v854.py             latest/investment_score_source_cache_latest.csv             latest/investment_score_source_run_log_latest.txt             latest/financial_valuation_cache_latest.csv             latest/investment_score_temp_source_refresh_v8115_summary_latest.json             > /tmp/v8116_protected.before

          find api/two_table_v1 -maxdepth 1 -type f -name '*.json' -print0             | sort -z | xargs -0 sha256sum             > /tmp/v8116_api.before

      - name: Generate staged candidate and frozen evidence
        shell: bash
        run: |
          cat > investment_score_source_contract_candidate_generator_v8116.py <<'PY'
          #!/usr/bin/env python3
          from __future__ import annotations

          import difflib
          import hashlib
          import json
          from datetime import datetime
          from pathlib import Path
          from zoneinfo import ZoneInfo

          VERSION = "2026-09-16-v8.11.6-freeze-v8115-stage-production-candidate"
          V8115_VERSION = "2026-09-16-v8.11.5-temp-source-enricher-full-refresh-regression"
          V8115_RESULT_COMMIT = "c6758c8f9a269d2907ee17fb1726fb3de37362dc"
          BASE_SOURCE_CONTRACT = "2026-09-10-v8.5.4-investment-score-source-contract"
          CANDIDATE_SOURCE_CONTRACT = (
              "2026-09-16-v8.11.6-staged-five-financial-cis-interest-revenue-contract"
          )

          ROOT = Path(".")
          KST = ZoneInfo("Asia/Seoul")

          BASE = ROOT / "investment_score_source_enricher_v854.py"
          V8115 = ROOT / "latest/investment_score_temp_source_refresh_v8115_summary_latest.json"

          CANDIDATE_DIR = ROOT / "staged"
          CANDIDATE = CANDIDATE_DIR / "investment_score_source_enricher_v8116_candidate.py"
          PATCH = CANDIDATE_DIR / "investment_score_source_enricher_v8116_candidate.patch"

          OUT_JSON = ROOT / "latest/investment_score_source_contract_candidate_v8116_summary_latest.json"
          OUT_LOG = ROOT / "latest/investment_score_source_contract_candidate_v8116_run_log_latest.txt"
          OUT_DOC = ROOT / "docs/investment_score_source_contract_candidate_v8116.md"

          AUDITED = {"000810", "005830", "029780", "105560", "138930"}
          EXPECTED_ACTIVE = {"000810", "005830", "029780", "105560"}
          EXPECTED_INACTIVE = {"138930"}
          EXACT_STATEMENT = "CIS"
          EXACT_ACCOUNT_ID = "ifrs-full_RevenueFromInterest"


          def sha256(path: Path) -> str:
              return hashlib.sha256(path.read_bytes()).hexdigest()


          def read_json(path: Path):
              return json.loads(path.read_text(encoding="utf-8-sig"))


          def validate_v8115():
              s = read_json(V8115)
              if s.get("version") != V8115_VERSION:
                  raise RuntimeError("V8116_V8115_VERSION_MISMATCH")
              if s.get("status") != "AUDIT_ONLY":
                  raise RuntimeError("V8116_V8115_STATUS_MISMATCH")
              if s.get("patch_status") != "TEMP_PATCH_EXECUTED_ONLY":
                  raise RuntimeError("V8116_V8115_PATCH_STATUS_MISMATCH")
              if set(s.get("audited_scope_tickers") or []) != AUDITED:
                  raise RuntimeError("V8116_V8115_AUDITED_SCOPE_CHANGED")
              if set(s.get("active_source_target_tickers") or []) != EXPECTED_ACTIVE:
                  raise RuntimeError("V8116_V8115_ACTIVE_SCOPE_CHANGED")
              if set(s.get("inactive_target_tickers") or []) != EXPECTED_INACTIVE:
                  raise RuntimeError("V8116_V8115_INACTIVE_SCOPE_CHANGED")

              exact = {
                  "control_source_rows": 242,
                  "shadow_source_rows": 242,
                  "control_source_raw_ready": 222,
                  "shadow_source_raw_ready": 226,
                  "raw_ready_delta": 4,
                  "scorer_universe_count": 221,
                  "control_ready_count": 5,
                  "shadow_ready_count": 8,
                  "ready_delta": 3,
                  "control_blocker_occurrences": 2178,
                  "shadow_blocker_occurrences": 2166,
                  "blocker_occurrences_reduced_by": 12,
                  "non_target_score_row_changed_count": 0,
              }
              for key, expected in exact.items():
                  if s.get(key) != expected:
                      raise RuntimeError(
                          f"V8116_V8115_EVIDENCE_CHANGED:{key}:{s.get(key)}!={expected}"
                      )

              if set(s.get("newly_ready_tickers") or []) != {"000810", "005830", "105560"}:
                  raise RuntimeError("V8116_V8115_NEW_READY_CHANGED")
              if s.get("lost_ready_tickers") != []:
                  raise RuntimeError("V8116_V8115_READY_REGRESSION_PRESENT")

              card = (s.get("target_score_results") or {}).get("029780") or {}
              if card.get("missing_after") != [
                  "수급·공시부담:SUPPLY_LIMITED_NO_POSITIVE_EVIDENCE"
              ]:
                  raise RuntimeError("V8116_V8115_CARD_REMAINDER_CHANGED")

              guards = s.get("hard_guards") or {}
              false_guards = {
                  "production_source_enricher_modified",
                  "production_source_cache_modified",
                  "production_source_run_log_modified",
                  "production_financial_cache_modified",
                  "production_api_modified",
                  "scoring_policy_modified",
                  "inactive_target_force_inserted",
                  "broad_financial_sector_exception_created",
                  "fuzzy_cis_revenue_match_allowed",
                  "source_value_imputed",
              }
              if not all(guards.get(k) is False for k in false_guards):
                  raise RuntimeError("V8116_V8115_HARD_GUARD_FALSE_SET_CHANGED")
              if guards.get("non_target_source_rows_unchanged") is not True:
                  raise RuntimeError("V8116_V8115_NON_TARGET_SOURCE_DRIFT")
              if guards.get("non_target_score_rows_unchanged") is not True:
                  raise RuntimeError("V8116_V8115_NON_TARGET_SCORE_DRIFT")
              if guards.get("ready_regression_count") != 0:
                  raise RuntimeError("V8116_V8115_READY_REGRESSION_COUNT_CHANGED")
              return s


          def replace_once(text: str, old: str, new: str, label: str) -> str:
              count = text.count(old)
              if count != 1:
                  raise RuntimeError(f"V8116_PATCH_ANCHOR_{label}:{count}")
              return text.replace(old, new, 1)


          def build_candidate(base_text: str) -> str:
              text = base_text

              text = replace_once(
                  text,
                  'SCRIPT_VERSION = "investment_score_source_enricher_v854.py v1.0.1-transient-retention-guard"',
                  'SCRIPT_VERSION = "investment_score_source_enricher_v8116_candidate.py staged-v1"',
                  "SCRIPT_VERSION",
              )
              text = replace_once(
                  text,
                  'SOURCE_CONTRACT_VERSION = "2026-09-10-v8.5.4-investment-score-source-contract"',
                  f'SOURCE_CONTRACT_VERSION = "{CANDIDATE_SOURCE_CONTRACT}"',
                  "SOURCE_CONTRACT_VERSION",
              )

              anchor = 'VALID_FINANCIAL_STATUS = {"READY", "PARTIAL"}\n'
              addition = '''VALID_FINANCIAL_STATUS = {"READY", "PARTIAL"}

          # V8.11.6 staged candidate: evidence-approved exact revenue semantics only.
          # This is intentionally ticker-scoped and fail-closed.
          FINANCIAL_REVENUE_CIS_EXACT_TICKERS = {
              "000810",
              "005830",
              "029780",
              "105560",
              "138930",
          }
          FINANCIAL_REVENUE_CIS_EXACT_STATEMENT = "CIS"
          FINANCIAL_REVENUE_CIS_EXACT_ACCOUNT_ID = "ifrs-full_RevenueFromInterest"
          '''
              text = replace_once(text, anchor, addition, "CONSTANTS")

              start = text.index("def query_multi_period(\n")
              end = text.index("\ndef select_fs_rows(", start)

              new_query = r'''def exact_cis_interest_revenue_hits(
              rows: Sequence[Mapping[str, Any]],
          ) -> List[Dict[str, Any]]:
              return [
                  dict(row)
                  for row in rows
                  if norm_text(row.get("sj_div")) == FINANCIAL_REVENUE_CIS_EXACT_STATEMENT
                  and norm_text(row.get("account_id")) == FINANCIAL_REVENUE_CIS_EXACT_ACCOUNT_ID
              ]


          def query_multi_period(
              client: OpenDartClient,
              targets: Sequence[Mapping[str, str]],
              year: int,
              report_code: str,
          ) -> Dict[str, Dict[str, List[Dict[str, Any]]]]:
              result: Dict[str, Dict[str, List[Dict[str, Any]]]] = defaultdict(
                  lambda: defaultdict(list)
              )
              corp_codes = [item["corp_code"] for item in targets]
              for batch_index, batch in enumerate(chunks(corp_codes, 100), start=1):
                  payload = client.get_json(
                      MULTI_ACCOUNT_URL,
                      {
                          "corp_code": ",".join(batch),
                          "bsns_year": str(year),
                          "reprt_code": report_code,
                      },
                      f"multi:{year}:{report_code}:batch{batch_index}",
                  )
                  items = payload.get("list") if isinstance(payload.get("list"), list) else []
                  for item in items:
                      corp_code = clean_corp_code(item.get("corp_code"))
                      fs_div = norm_text(item.get("fs_div"))
                      if corp_code and fs_div:
                          result[corp_code][fs_div].append(dict(item))
                  time.sleep(0.08)

              # Narrow staged extension:
              # only audited tickers receive one exact CIS interest-revenue row from
              # the official full-account endpoint. No fuzzy alias or sector-wide rule.
              for target in targets:
                  ticker = clean_ticker(target.get("ticker"))
                  if ticker not in FINANCIAL_REVENUE_CIS_EXACT_TICKERS:
                      continue

                  corp_code = clean_corp_code(target.get("corp_code"))
                  fs_div = norm_text(target.get("preferred_fs_div")) or "CFS"
                  if not corp_code:
                      continue

                  payload = client.get_json(
                      FULL_ACCOUNT_URL,
                      {
                          "corp_code": corp_code,
                          "bsns_year": str(year),
                          "reprt_code": report_code,
                          "fs_div": fs_div,
                      },
                      f"narrow-revenue:{ticker}:{year}:{report_code}:{fs_div}",
                  )
                  status = norm_text(payload.get("status"))
                  items = payload.get("list") if isinstance(payload.get("list"), list) else []
                  if status != "000":
                      continue

                  hits = exact_cis_interest_revenue_hits(items)
                  if len(hits) != 1:
                      # Fail closed: ambiguous or absent exact account is not promoted.
                      continue

                  selected_rows = result[corp_code][fs_div]
                  preexisting = exact_cis_interest_revenue_hits(selected_rows)
                  if len(preexisting) > 1:
                      raise RuntimeError(
                          "AMBIGUOUS_PREEXISTING_EXACT_CIS_INTEREST_REVENUE:"
                          f"{ticker}:{year}:{report_code}:{len(preexisting)}"
                      )
                  if not preexisting:
                      selected_rows.append(hits[0])

              return result

          '''
              text = text[:start] + new_query + text[end + 1:]

              start = text.index("def account_values(")
              end = text.index("\ndef cumulative_value(", start)

              new_accounts = r'''def account_values(
              rows: Sequence[Mapping[str, Any]],
              ticker: str = "",
          ) -> Dict[str, Dict[str, Any]]:
              output: Dict[str, Dict[str, Any]] = {}
              ticker = clean_ticker(ticker)

              for key, spec in ACCOUNT_SPECS.items():
                  chosen = None

                  if key == "revenue" and ticker in FINANCIAL_REVENUE_CIS_EXACT_TICKERS:
                      exact_hits = exact_cis_interest_revenue_hits(rows)
                      if len(exact_hits) == 1:
                          chosen = exact_hits[0]
                      elif len(exact_hits) > 1:
                          # Fail closed; never guess among duplicate exact rows.
                          chosen = None
                      else:
                          chosen = choose_account(rows, spec)
                  else:
                      chosen = choose_account(rows, spec)

                  if chosen is None:
                      output[key] = {"found": False}
                      continue

                  output[key] = {
                      "found": True,
                      "account_nm": norm_text(chosen.get("account_nm")),
                      "thstrm_amount": parse_number(chosen.get("thstrm_amount")),
                      "thstrm_add_amount": parse_number(chosen.get("thstrm_add_amount")),
                      "frmtrm_amount": parse_number(chosen.get("frmtrm_amount")),
                      "frmtrm_add_amount": parse_number(chosen.get("frmtrm_add_amount")),
                      "bfefrmtrm_amount": parse_number(chosen.get("bfefrmtrm_amount")),
                  }
              return output

          '''
              text = text[:start] + new_accounts + text[end + 1:]

              text = replace_once(
                  text,
                  "annual_accounts = account_values(annual_rows)",
                  'annual_accounts = account_values(annual_rows, target["ticker"])',
                  "ANNUAL_CALL",
              )
              text = replace_once(
                  text,
                  "period_accounts[period] = account_values(rows)",
                  'period_accounts[period] = account_values(rows, target["ticker"])',
                  "QUARTER_CALL",
              )

              return text


          def main():
              for path in (BASE, V8115):
                  if not path.is_file():
                      raise RuntimeError("V8116_MISSING_INPUT:" + str(path))

              v8115 = validate_v8115()

              base_text = BASE.read_text(encoding="utf-8")
              if f'SOURCE_CONTRACT_VERSION = "{BASE_SOURCE_CONTRACT}"' not in base_text:
                  raise RuntimeError("V8116_BASE_SOURCE_CONTRACT_CHANGED")
              if '"statement": "IS"' not in base_text:
                  raise RuntimeError("V8116_BASE_REVENUE_STATEMENT_CHANGED")
              if '"이자수익"' not in base_text:
                  raise RuntimeError("V8116_BASE_INTEREST_EXCLUSION_CHANGED")

              candidate_text = build_candidate(base_text)

              CANDIDATE_DIR.mkdir(parents=True, exist_ok=True)
              CANDIDATE.write_text(candidate_text, encoding="utf-8")

              diff = "".join(
                  difflib.unified_diff(
                      base_text.splitlines(keepends=True),
                      candidate_text.splitlines(keepends=True),
                      fromfile=str(BASE),
                      tofile=str(CANDIDATE),
                  )
              )
              PATCH.write_text(diff, encoding="utf-8")

              if not diff:
                  raise RuntimeError("V8116_EMPTY_PATCH")
              if "ifrs-full_RevenueFromInterest" not in candidate_text:
                  raise RuntimeError("V8116_EXACT_ACCOUNT_MISSING")
              if "FINANCIAL_REVENUE_CIS_EXACT_TICKERS" not in candidate_text:
                  raise RuntimeError("V8116_TICKER_SCOPE_MISSING")

              summary = {
                  "version": VERSION,
                  "v8115_version": V8115_VERSION,
                  "v8115_result_commit": V8115_RESULT_COMMIT,
                  "generated_at_kst": datetime.now(KST).isoformat(timespec="seconds"),
                  "status": "STAGED_CANDIDATE_ONLY",
                  "base_source_contract_version": BASE_SOURCE_CONTRACT,
                  "candidate_source_contract_version": CANDIDATE_SOURCE_CONTRACT,
                  "base_source_sha256": sha256(BASE),
                  "candidate_source_sha256": sha256(CANDIDATE),
                  "patch_sha256": sha256(PATCH),
                  "audited_scope_tickers": sorted(AUDITED),
                  "v8115_frozen_evidence": {
                      "source_rows": v8115["control_source_rows"],
                      "raw_ready_before": v8115["control_source_raw_ready"],
                      "raw_ready_after": v8115["shadow_source_raw_ready"],
                      "raw_ready_delta": v8115["raw_ready_delta"],
                      "scorer_universe_count": v8115["scorer_universe_count"],
                      "ready_before": v8115["control_ready_count"],
                      "ready_after": v8115["shadow_ready_count"],
                      "ready_delta": v8115["ready_delta"],
                      "blockers_before": v8115["control_blocker_occurrences"],
                      "blockers_after": v8115["shadow_blocker_occurrences"],
                      "blockers_reduced_by": v8115["blocker_occurrences_reduced_by"],
                      "newly_ready_tickers": v8115["newly_ready_tickers"],
                  },
                  "candidate_contract": {
                      "account_key": "revenue",
                      "statement": EXACT_STATEMENT,
                      "account_id": EXACT_ACCOUNT_ID,
                      "selection_rule": "EXACT_ID_UNIQUE_ONLY",
                      "ticker_scope": sorted(AUDITED),
                      "inactive_ticker_force_insert": False,
                      "fallback_for_no_exact_hit": "UNCHANGED_V854_REVENUE_LOGIC",
                      "duplicate_exact_hit_policy": "FAIL_CLOSED_NO_SELECTION",
                      "full_account_fetch_scope": (
                          "AUDITED_TICKERS_PRESENT_IN_CURRENT_TARGETS_ONLY"
                      ),
                  },
                  "hard_guards": {
                      "production_source_enricher_modified": False,
                      "production_source_cache_modified": False,
                      "production_api_modified": False,
                      "scoring_policy_modified": False,
                      "broad_financial_sector_rule_created": False,
                      "new_fuzzy_alias_created": False,
                      "inactive_target_force_inserted": False,
                      "source_value_imputed": False,
                  },
                  "next_step": (
                      "STAGED_PRODUCTION_PATCH_READY_FOR_CONTROLLED_APPLY_V8117"
                  ),
              }

              OUT_JSON.write_text(
                  json.dumps(summary, ensure_ascii=False, indent=2) + "\n",
                  encoding="utf-8",
              )

              OUT_LOG.write_text(
                  "\n".join([
                      f"VERSION={VERSION}",
                      "STATUS=STAGED_CANDIDATE_ONLY",
                      f"V8115_RESULT_COMMIT={V8115_RESULT_COMMIT}",
                      "V8115_EVIDENCE_FROZEN=true",
                      "AUDITED_SCOPE=5",
                      "EXACT_STATEMENT=CIS",
                      f"EXACT_ACCOUNT_ID={EXACT_ACCOUNT_ID}",
                      "SELECTION_RULE=EXACT_ID_UNIQUE_ONLY",
                      "DUPLICATE_POLICY=FAIL_CLOSED_NO_SELECTION",
                      "FALLBACK=UNCHANGED_V854_REVENUE_LOGIC",
                      "PRODUCTION_SOURCE_ENRICHER_MODIFIED=false",
                      "PRODUCTION_SOURCE_CACHE_MODIFIED=false",
                      "PRODUCTION_API_MODIFIED=false",
                      "SCORING_POLICY_MODIFIED=false",
                      "STATUS_OK=true",
                  ]) + "\n",
                  encoding="utf-8",
              )

              OUT_DOC.parent.mkdir(parents=True, exist_ok=True)
              OUT_DOC.write_text(
                  "\n".join([
                      "# V8.11.6 staged source-contract candidate",
                      "",
                      f"- Version: `{VERSION}`",
                      f"- Frozen V8.11.5 result commit: `{V8115_RESULT_COMMIT}`",
                      "- Production `investment_score_source_enricher_v854.py` is unchanged.",
                      "- Candidate is generated as a separate staged file.",
                      "- Scope is exactly five audited tickers.",
                      f"- Exact selector: `{EXACT_STATEMENT}` / `{EXACT_ACCOUNT_ID}`.",
                      "- Duplicate exact rows fail closed.",
                      "- No exact hit falls back to unchanged V8.5.4 revenue selection.",
                      "- Non-audited tickers always use unchanged V8.5.4 logic.",
                      "",
                      "## Frozen V8.11.5 proof",
                      "",
                      "- Source RAW ready: 222 → 226 (+4).",
                      "- Scorer READY: 5 → 8 (+3).",
                      "- Blockers: 2178 → 2166 (-12).",
                      "- Non-target source/score drift: 0.",
                      "",
                  ]),
                  encoding="utf-8",
              )

              print("V8116_CANDIDATE_GENERATION=PASS")


          if __name__ == "__main__":
              main()
          PY

          python -m py_compile             investment_score_source_contract_candidate_generator_v8116.py
          python investment_score_source_contract_candidate_generator_v8116.py             | tee /tmp/v8116_generator.txt
          grep -q 'V8116_CANDIDATE_GENERATION=PASS'             /tmp/v8116_generator.txt

      - name: Compile candidate and run self-test
        shell: bash
        run: |
          set -euo pipefail
          python -m py_compile             staged/investment_score_source_enricher_v8116_candidate.py

          python staged/investment_score_source_enricher_v8116_candidate.py             --self-test | tee /tmp/v8116_candidate_selftest.txt
          grep -q 'SELF_TEST=PASS' /tmp/v8116_candidate_selftest.txt
          grep -q 'SCORE_VALUES_GENERATED=false'             /tmp/v8116_candidate_selftest.txt
          grep -q 'NET_CASH_CALCULATED=false'             /tmp/v8116_candidate_selftest.txt
          grep -q 'EV_EBITDA_CALCULATED=false'             /tmp/v8116_candidate_selftest.txt

      - name: Run deterministic contract tests
        shell: bash
        run: |
          cat > investment_score_source_contract_candidate_test_v8116.py <<'PY'
          #!/usr/bin/env python3
          from __future__ import annotations

          import importlib.util
          import json
          from pathlib import Path

          BASE_PATH = Path("investment_score_source_enricher_v854.py")
          CANDIDATE_PATH = Path("staged/investment_score_source_enricher_v8116_candidate.py")
          SUMMARY = Path(
              "latest/investment_score_source_contract_candidate_v8116_summary_latest.json"
          )

          AUDITED = "000810"
          NON_AUDITED = "005930"


          def load_module(path: Path, name: str):
              spec = importlib.util.spec_from_file_location(name, path)
              if spec is None or spec.loader is None:
                  raise RuntimeError("MODULE_SPEC_FAILED:" + str(path))
              module = importlib.util.module_from_spec(spec)
              spec.loader.exec_module(module)
              return module


          def row(
              sj_div,
              account_id,
              account_nm,
              amount,
              add=None,
              prev=None,
              prev_add=None,
              prev2=None,
          ):
              return {
                  "sj_div": sj_div,
                  "account_id": account_id,
                  "account_nm": account_nm,
                  "thstrm_amount": str(amount),
                  "thstrm_add_amount": str(add if add is not None else amount),
                  "frmtrm_amount": str(prev if prev is not None else amount - 10),
                  "frmtrm_add_amount": str(
                      prev_add
                      if prev_add is not None
                      else (prev if prev is not None else amount - 10)
                  ),
                  "bfefrmtrm_amount": str(
                      prev2 if prev2 is not None else amount - 20
                  ),
              }


          class FakeClient:
              def __init__(self, full_rows_by_corp):
                  self.full_rows_by_corp = full_rows_by_corp
                  self.calls = []

              def get_json(self, url, params, label):
                  self.calls.append((url, dict(params), label))
                  if "fnlttMultiAcnt" in url:
                      items = []
                      for corp in str(params.get("corp_code", "")).split(","):
                          if not corp:
                              continue
                          items.append({
                              "corp_code": corp,
                              "fs_div": "CFS",
                              **row(
                                  "IS",
                                  "ifrs-full_Revenue",
                                  "매출액",
                                  100,
                                  100,
                                  90,
                                  90,
                                  80,
                              ),
                          })
                      return {"status": "000", "list": items}

                  corp = str(params.get("corp_code", ""))
                  return {
                      "status": "000",
                      "list": self.full_rows_by_corp.get(corp, []),
                  }


          def main():
              base = load_module(BASE_PATH, "base_v854_for_v8116_test")
              cand = load_module(CANDIDATE_PATH, "candidate_v8116_test")

              is_revenue = row(
                  "IS",
                  "ifrs-full_Revenue",
                  "매출액",
                  100,
                  100,
                  90,
                  90,
                  80,
              )
              cis_interest = row(
                  "CIS",
                  "ifrs-full_RevenueFromInterest",
                  "이자수익",
                  200,
                  200,
                  180,
                  180,
                  160,
              )
              op = row(
                  "IS",
                  "ifrs-full_ProfitLossFromOperatingActivities",
                  "영업이익",
                  30,
                  30,
                  20,
                  20,
                  10,
              )

              audited = cand.account_values(
                  [is_revenue, cis_interest, op],
                  AUDITED,
              )
              assert audited["revenue"]["found"] is True
              assert audited["revenue"]["account_nm"] == "이자수익"
              assert audited["revenue"]["thstrm_amount"] == 200.0

              non_audited = cand.account_values(
                  [is_revenue, cis_interest, op],
                  NON_AUDITED,
              )
              assert non_audited["revenue"]["found"] is True
              assert non_audited["revenue"]["account_nm"] == "매출액"
              assert non_audited["revenue"]["thstrm_amount"] == 100.0

              duplicate = dict(cis_interest)
              duplicate["account_nm"] = "이자수익_중복"
              dup = cand.account_values(
                  [is_revenue, cis_interest, duplicate, op],
                  AUDITED,
              )
              assert dup["revenue"]["found"] is False

              fallback = cand.account_values([is_revenue, op], AUDITED)
              base_fallback = base.account_values([is_revenue, op])
              assert fallback["revenue"] == base_fallback["revenue"]

              assert audited["operating_profit"]["account_nm"] == "영업이익"
              assert audited["operating_profit"]["thstrm_amount"] == 30.0

              full_rows = {
                  "00000001": [dict(cis_interest)],
                  "00000002": [dict(cis_interest)],
              }
              client = FakeClient(full_rows)
              targets = [
                  {
                      "ticker": AUDITED,
                      "corp_code": "00000001",
                      "preferred_fs_div": "CFS",
                  },
                  {
                      "ticker": NON_AUDITED,
                      "corp_code": "00000002",
                      "preferred_fs_div": "CFS",
                  },
              ]
              result = cand.query_multi_period(client, targets, 2025, "11011")

              full_calls = [
                  call for call in client.calls
                  if "fnlttSinglAcntAll" in call[0]
              ]
              assert len(full_calls) == 1
              assert full_calls[0][1]["corp_code"] == "00000001"

              audited_rows = result["00000001"]["CFS"]
              non_audited_rows = result["00000002"]["CFS"]
              assert len(cand.exact_cis_interest_revenue_hits(audited_rows)) == 1
              assert len(cand.exact_cis_interest_revenue_hits(non_audited_rows)) == 0

              s = json.loads(SUMMARY.read_text(encoding="utf-8"))
              assert s["status"] == "STAGED_CANDIDATE_ONLY"
              assert s["candidate_contract"]["selection_rule"] == "EXACT_ID_UNIQUE_ONLY"
              assert s["candidate_contract"]["duplicate_exact_hit_policy"] == (
                  "FAIL_CLOSED_NO_SELECTION"
              )
              assert s["hard_guards"]["production_source_enricher_modified"] is False

              print("V8116_DETERMINISTIC_CONTRACT_TESTS=PASS")


          if __name__ == "__main__":
              main()
          PY

          python -m py_compile             investment_score_source_contract_candidate_test_v8116.py
          python investment_score_source_contract_candidate_test_v8116.py             | tee /tmp/v8116_contract_tests.txt
          grep -q 'V8116_DETERMINISTIC_CONTRACT_TESTS=PASS'             /tmp/v8116_contract_tests.txt

      - name: Validate staged candidate outputs
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          import json
          from pathlib import Path

          s = json.loads(
              Path(
                  "latest/investment_score_source_contract_candidate_v8116_summary_latest.json"
              ).read_text(encoding="utf-8")
          )

          audited = {"000810","005830","029780","105560","138930"}

          assert s["status"] == "STAGED_CANDIDATE_ONLY"
          assert (
              s["v8115_result_commit"]
              == "c6758c8f9a269d2907ee17fb1726fb3de37362dc"
          )
          assert set(s["audited_scope_tickers"]) == audited

          proof = s["v8115_frozen_evidence"]
          assert proof["source_rows"] == 242
          assert proof["raw_ready_before"] == 222
          assert proof["raw_ready_after"] == 226
          assert proof["raw_ready_delta"] == 4
          assert proof["scorer_universe_count"] == 221
          assert proof["ready_before"] == 5
          assert proof["ready_after"] == 8
          assert proof["ready_delta"] == 3
          assert proof["blockers_before"] == 2178
          assert proof["blockers_after"] == 2166
          assert proof["blockers_reduced_by"] == 12
          assert set(proof["newly_ready_tickers"]) == {
              "000810","005830","105560"
          }

          c = s["candidate_contract"]
          assert c["statement"] == "CIS"
          assert c["account_id"] == "ifrs-full_RevenueFromInterest"
          assert c["selection_rule"] == "EXACT_ID_UNIQUE_ONLY"
          assert c["inactive_ticker_force_insert"] is False
          assert c["fallback_for_no_exact_hit"] == (
              "UNCHANGED_V854_REVENUE_LOGIC"
          )
          assert c["duplicate_exact_hit_policy"] == (
              "FAIL_CLOSED_NO_SELECTION"
          )

          h = s["hard_guards"]
          assert all(v is False for v in h.values())

          candidate = Path(
              "staged/investment_score_source_enricher_v8116_candidate.py"
          ).read_text(encoding="utf-8")
          assert (
              'SOURCE_CONTRACT_VERSION = '
              '"2026-09-16-v8.11.6-staged-five-financial-cis-interest-revenue-contract"'
              in candidate
          )
          assert "FINANCIAL_REVENUE_CIS_EXACT_TICKERS" in candidate
          assert "ifrs-full_RevenueFromInterest" in candidate
          assert (
              'annual_accounts = account_values(annual_rows, target["ticker"])'
              in candidate
          )
          assert (
              'period_accounts[period] = account_values(rows, target["ticker"])'
              in candidate
          )

          patch = Path(
              "staged/investment_score_source_enricher_v8116_candidate.patch"
          ).read_text(encoding="utf-8")
          assert patch
          assert "ifrs-full_RevenueFromInterest" in patch
          print("V8116_OUTPUT_VALIDATION=PASS")
          PY

      - name: Verify production files unchanged
        shell: bash
        run: |
          set -euo pipefail

          sha256sum             config/investment_score_policy_v880.json             investment_score_source_enricher_v854.py             latest/investment_score_source_cache_latest.csv             latest/investment_score_source_run_log_latest.txt             latest/financial_valuation_cache_latest.csv             latest/investment_score_temp_source_refresh_v8115_summary_latest.json             > /tmp/v8116_protected.after
          diff -u /tmp/v8116_protected.before /tmp/v8116_protected.after

          find api/two_table_v1 -maxdepth 1 -type f -name '*.json' -print0             | sort -z | xargs -0 sha256sum             > /tmp/v8116_api.after
          diff -u /tmp/v8116_api.before /tmp/v8116_api.after

          echo "V8116_PRODUCTION_FILES_UNCHANGED=PASS"

      - name: Restrict changed files
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          import subprocess

          allowed = {
              "investment_score_source_contract_candidate_generator_v8116.py",
              "investment_score_source_contract_candidate_test_v8116.py",
              "staged/investment_score_source_enricher_v8116_candidate.py",
              "staged/investment_score_source_enricher_v8116_candidate.patch",
              "latest/investment_score_source_contract_candidate_v8116_summary_latest.json",
              "latest/investment_score_source_contract_candidate_v8116_run_log_latest.txt",
              "docs/investment_score_source_contract_candidate_v8116.md",
          }
          changed = {
              line[3:]
              for line in subprocess.check_output(
                  ["git","status","--porcelain"], text=True
              ).splitlines()
              if line
          }
          unexpected = sorted(changed - allowed)
          if unexpected:
              raise SystemExit(
                  "UNEXPECTED_CHANGED_FILES:" + ",".join(unexpected)
              )
          print("V8116_CHANGED_FILES_RESTRICTED=PASS")
          PY

      - name: Refuse concurrent protected input drift
        shell: bash
        run: |
          set -euo pipefail
          git fetch origin main

          python - <<'PY'
          import subprocess

          protected = {
              "config/investment_score_policy_v880.json",
              "investment_score_source_enricher_v854.py",
              "latest/investment_score_source_cache_latest.csv",
              "latest/investment_score_source_run_log_latest.txt",
              "latest/financial_valuation_cache_latest.csv",
              "latest/investment_score_temp_source_refresh_v8115_summary_latest.json",
          }

          upstream = set(
              subprocess.check_output(
                  ["git","diff","--name-only","HEAD..origin/main"],
                  text=True,
              ).splitlines()
          )
          dangerous = sorted(
              p for p in upstream
              if p in protected or p.startswith("api/two_table_v1/")
          )
          if dangerous:
              raise SystemExit(
                  "CONCURRENT_PROTECTED_INPUT_DRIFT:"
                  + ",".join(dangerous)
              )
          print("V8116_NO_CONCURRENT_PROTECTED_INPUT_DRIFT=PASS")
          PY

      - name: Commit and push staged candidate evidence only
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions"
          git config user.email "github-actions@users.noreply.github.com"

          git add             investment_score_source_contract_candidate_generator_v8116.py             investment_score_source_contract_candidate_test_v8116.py             staged/investment_score_source_enricher_v8116_candidate.py             staged/investment_score_source_enricher_v8116_candidate.patch             latest/investment_score_source_contract_candidate_v8116_summary_latest.json             latest/investment_score_source_contract_candidate_v8116_run_log_latest.txt             docs/investment_score_source_contract_candidate_v8116.md

          if git diff --cached --quiet; then
            echo "NO_CHANGES_TO_COMMIT"
            exit 0
          fi

          git commit -m "Stage narrow revenue source contract candidate v8.11.6"
          git pull --rebase origin main
          git push origin HEAD:main
